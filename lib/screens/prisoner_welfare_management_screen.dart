import 'package:flutter/material.dart';

class PrisonerWelfareManagementScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Prisoner Welfare Management"),
      ),
      body: Center(
        child: Text("Prisoner Welfare Management Module"),
      ),
    );
  }
}
